apiVersion: v1
kind: Service
metadata:
  name: ai-concierge-backend-svc
  namespace: default
spec:
  selector:
    app: ai-concierge-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer             # Gives you a public IP on GKE/EKS/AKS


